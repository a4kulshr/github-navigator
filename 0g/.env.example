# REQUIRED - Your wallet private key (without 0x prefix)
# Get from: npm run setup (0G network setup)
PRIVATE_KEY=your-private-key-here

# Server config
PORT=4000
NODE_ENV=development

# 0G Compute AI Credentials (REQUIRED for AI analysis)
# Get these from: npm install -g @0glabs/0g-compute-cli && 0g-compute-cli login
# OR visit: https://build.0g.ai
ZG_COMPUTE_API_KEY=your-api-key-here
ZG_COMPUTE_URL=https://your-0g-endpoint

# Flask secret key
FLASK_SECRET_KEY=your-secret-key-here
